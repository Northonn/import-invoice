services:
  pdf-invoice-api:
    build:
      context: .
      args:
        PDFBOX_VERSION: "3.0.5"
    environment:
      API_KEY: "troque-esta-chave"
      MAX_UPLOAD_MB: "25"
      PDFBOX_TIMEOUT_SECONDS: "60"
    ports:
      - "8000:8000"
    restart: unless-stopped
